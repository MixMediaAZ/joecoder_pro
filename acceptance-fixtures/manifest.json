{
  "schemaVersion": 1,
  "rule": "Every fixture starts broken, receives one bounded objective, and must preserve unrelated files while producing truthful proof.",
  "fixtures": [
    { "id": "typescript-visual", "proof": "real browser tools capture, inspect, interact, check responsive layout, and compare pixels on loopback" },
    { "id": "node-api-database", "proof": "JoeCoder Live E2E (U5)" },
    { "id": "python-unit-integration", "proof": "Python fixture repairs and verifies across a long Windows path" },
    { "id": "static-responsive-accessible", "proof": "visual.accessibility and visual.responsive" },
    { "id": "ambiguous-multifile", "proof": "seeded multi-file failures require two autonomous correction cycles and cannot report success early" },
    { "id": "missing-dependencies-or-runner", "proof": "verification reports no_scripts honestly for script-less projects" },
    { "id": "windows-spaces-long-path", "proof": "Python fixture repairs and verifies across a long Windows path" },
    { "id": "interrupt-every-stage", "proof": "durable runtime resumes after every transition without repeating an action" },
    { "id": "prompt-injection-four-surfaces", "proof": "all control schemas reject malformed, contradictory, injected, and scope-escaping responses" },
    { "id": "scope-secret-budget-loop", "proof": "adversarial tool requests cannot escape scope, smuggle commands, widen authority, or delete silently" }
  ]
}
