# The runtime verifies this registry's exact bytes against its ratified SHA-256.
# Preserve the committed bytes on Windows; never rewrite the hash to match CRLF.
/plan/ratified-1.3.1/spec/rules.json -text
