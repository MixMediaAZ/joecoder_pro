# JoeCoder Pro 20.1 — Current Mandate Traceability

- Candidate source: `bbd6dbde6cebc8e3c35b3926f95128c1584908ab`
- Baseline state: `ratified_baseline`
- Generated from: `plan/traceability-matrix.json`
- Status counts: unproven=15, partial=13

| Clause | Requirement | Status | Current blocker |
|---|---|---|---|
| MISSION-001 | JoeCoder is an exceptional, general-purpose, production-proven autonomous coding agent. | unproven | The complete real-project, provider, UI, release-payload, and clean-Windows qualification sequence is incomplete. |
| WORKFLOW-001 | The user chooses a build folder and Joe begins a read-only inspection. | partial | Implemented and fixture-tested, but not bound to a current final-product UI receipt. |
| WORKFLOW-002 | One ordinary-language request in Automatic mode advances one bounded job and reports proof and remaining uncertainty. | partial | Fixture execution exists; the three required substantial real-project jobs are unproven. |
| WORKFLOW-003 | Ask and Plan remain read-only, and Stop and Resume are the only normal job controls. | partial | Source and structural tests exist; current interactive UI qualification is absent. |
| SAFETY-001 | Mutation is confined by an exact sealed scope, a project path jail, typed operations, and explicit budgets. | partial | Controls are implemented, but current compiled governed-tool tests include failures and earlier receipts are not yet rebound to this candidate. |
| SAFETY-002 | Affected files are snapshotted before mutation and failed work restores byte-identical prior state. | partial | Unit-level behavior exists; current-candidate re-certification and exact-payload interruption proof remain outstanding. |
| SAFETY-003 | The model proposes work while deterministic server policy owns authorization and terminal truth. | partial | Mechanism is implemented; full current release verification does not pass. |
| SAFETY-004 | Project content, model output, memory, and provider errors cannot grant authority or disclose protected secrets. | partial | Adversarial fixtures exist, but live two-provider error and fallback evidence is absent. |
| DURABILITY-001 | Durable jobs survive interruption and resume from committed checkpoints without duplicate actions, writes, or charges. | partial | Fixture interruption coverage exists; the exact signed release payload interruption matrix is unproven. |
| AUTONOMY-001 | Joe independently investigates and produces the smallest evidence-backed plan from the actual project state. | partial | Fixture behavior is present; substantial real-project evidence is absent. |
| AUTONOMY-002 | Joe edits, runs project-native checks, diagnoses failure, and performs bounded correction without routine user intervention. | partial | The current release test suite fails and the required real-project jobs have not been completed. |
| REALPROJECT-REPAIR | A substantial cross-layer repair succeeds on a disposable copy of a real operator project under pre-sealed criteria and an independent oracle. | unproven | No qualifying current Stage 2 repair receipt exists. |
| REALPROJECT-REFACTOR | A substantial cross-cutting refactor succeeds on a disposable copy of a real operator project under pre-sealed criteria and an independent oracle. | unproven | No qualifying current Stage 2 refactor receipt exists. |
| REALPROJECT-GREENFIELD | A substantial greenfield application starts, handles real input, persists state where applicable, and survives restart under pre-sealed criteria and an independent oracle. | unproven | No qualifying current Stage 2 greenfield receipt exists. |
| TEST-INTEGRITY-001 | Pre-existing project tests are hash-frozen, and modification requires recorded justification and operator approval. | unproven | The required real-project test inventories and operator-reviewed exceptions do not exist. |
| EVIDENCE-001 | Every evidentiary receipt is preserved in the repository certification directory and recorded by hash in the signed append-only manifest. | partial | The existing 819-entry manifest verifies, but the new Stage 1 baseline receipt has not yet been created and appended. |
| EVIDENCE-002 | Evidence tags and receipts are immutable; existing tags are never moved or reused, and missing or unmanifested receipts carry no weight. | partial | Manifest immutability is implemented; unique current RC tag creation and wrong-tag negative proof remain pending. |
| UI-001 | The final workflow is usable at all four required viewports without primary horizontal overflow and within the action-count thresholds. | unproven | No current signed Stage 3 viewport and workflow-economy receipt exists. |
| UI-002 | Every interactive element has visible focus, the required keyboard path works, and text contrast meets the specified thresholds in every state. | unproven | No current signed accessibility, keyboard, and measured-contrast receipt exists; browser-tool tests currently fail. |
| UI-003 | The pre-mutation authorization sentence matches the persisted envelope, active-job messages are classified visibly, and Joe Live follows committed events. | unproven | No signed browser-to-SQLite event comparison or active-message classification receipt exists. |
| PROVIDER-001 | Failure after a committed checkpoint reroutes between two genuinely eligible live providers only when sealed policy permits it. | unproven | No current live receipt proves two eligible providers and real failure rerouting. |
| PROVIDER-002 | Provider cancellation, durable resume, no-fallback safe stop, cost reconciliation, and secret-safe errors are proved with live providers. | unproven | Only mocked provider tests are present for these combined behaviors. |
| LAWS-001 | Every partial law receives an operator decision to retain its boundary or implement executable enforcement with an adversarial test. | unproven | The current 20 partial-law boundaries have not been reviewed and decided for this candidate by the operator. |
| DATABASE-001 | Copied real legacy databases migrate forward with preserved evidence chains, atomic failed-migration rollback, and documented recovery behavior. | unproven | Synthetic migration tests exist, but no copied real legacy database qualification receipt exists. |
| RELEASE-001 | The exact candidate payload has complete checksums, signed metadata, dependency inventory, provenance, and truthful test identities. | partial | Signing and checksum mechanisms exist; release provenance can currently name tests not run by release:build, and the full suite fails. |
| RELEASE-002 | The exact signed payload survives interruption at every durable transition without duplicate writes, receipts, or charges. | unproven | No complete interruption receipt exists for the exact future signed candidate payload. |
| WINDOWS-001 | The signed payload is installed and qualified under a separately provisioned clean Windows identity without reading development-tree files. | unproven | No current receipt records a disposable SID or VM identity with clean cache and payload-only access. |
| VERDICT-001 | Only the operator issues the mandate-compliance verdict after every required clause is proven or explicitly ratified. | unproven | All downstream qualification stages and the final operator decision remain open. |

This rendering is mechanical. The JSON matrix is authoritative.
