# Plan Amendment 1.3.1 — Semantic Proof and Broker Boundary

## Authorization and scope

The user explicitly directed correction of semantic copy-through and the broker-qualification sequencing contradiction. This amendment changes planning artifacts only. It creates no application scaffold, dependency installation, provider call, project mutation, or implementation authorization.

## Findings corrected

1. Eighty-eight M1–M9 Work Orders reused generic acceptance wording for implementation, negative testing, and evidence.
2. Fourteen unrelated tasks used only `FX-BUILD-001`, so syntactic traceability overstated semantic coverage.
3. The fixture inventory named scenarios but did not provide machine-readable expected and forbidden outcomes.
4. M1 scheduled broker IPC/file-service foundations while prose said M3 qualification occurred before all broker construction, creating an avoidable sequencing contradiction.
5. The validator checked field presence, counts, IDs, and graph shape but did not reject generic semantic copy-through.

## Ratified corrections

1. Preserve the 95-Work-Order graph and seven-task M0 boundary.
2. Replace all 88 generic M1–M9 acceptance sets with task-bound law, exit-proof, fixture-contract, and evidence criteria.
3. Add `spec/fixture-contracts.json` with a machine-readable oracle specification for each of the 84 admitted fixtures; construction must turn each assigned specification into an executable oracle before it can count as proof.
4. Replace the 15 identified irrelevant or underpowered fixture assignments with task-relevant adversarial sets and synchronize Work Order and rule proof chains.
5. Define M1 broker scope as authenticated, bounded, non-executing IPC and file-service groundwork only.
6. Gate all native process creation, project-command execution, restricted-token containment, Job Objects, AppContainer, network-isolation enforcement, and sandbox claims on successful JC19-M3-000 qualification.
7. Strengthen validation to reject the old generic phrases, duplicated acceptance semantics, incomplete fixture oracles, known irrelevant mappings, traceability drift, and sequencing-boundary drift.
8. Advance the effective plan patch version to 1.3.1 and regenerate ratification hashes and the validation report.

## Completion condition

This amendment is ratified only when every effective file hash matches the ratification manifest, all formal schemas compile, the invalid-instance corpus is rejected, all semantic checks pass, and the report still states that implementation is not authorized.
