{
  "schemaVersion": "1.3.0",
  "purpose": "Translate planning rollback categories into concrete generated Work Order strategies.",
  "mappings": [
    {
      "planning": "bootstrap_snapshot",
      "allowedGenerated": [
        "version_pointer"
      ],
      "rule": "A pre-mutation source snapshot and verified restore reference are mandatory."
    },
    {
      "planning": "version_pointer_or_journaled_materialization",
      "allowedGenerated": [
        "version_pointer",
        "journaled_materialization"
      ],
      "rule": "Select exactly one from the declared mutation target before authorization."
    },
    {
      "planning": "none_read_only",
      "allowedGenerated": [
        "none_read_only"
      ],
      "rule": "Only for operations proven read-only."
    }
  ]
}
